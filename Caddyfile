# Aceita tanto o domínio quanto qualquer IP
mobile-sps.site {
    reverse_proxy backend:8000
    encode gzip
    tls leokaique7@gmail.com
}

# Fallback para IPs (HTTP)
:80 {
    reverse_proxy backend:8000
    encode gzip
}

